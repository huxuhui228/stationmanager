{% extends './base.html' %}
<div>
{% block main %}
<div id="search_field">
{% include './searchField.html' %}
</div>
<div id="dataTable">
<table lay-filter="stationIndexTable">
    <thead>
        <tr>
            <th lay-data="{field:'number', width:80, sort: true}">序号</th>
            <th lay-data="{field:'name', width:200, sort: true}">名称</th>
            <th lay-data="{field:'sierialNum', width:80, sort: true}">编号</th>
            <th lay-data="{field:'measure_means', width:80, sort: true}">手段</th>
            <th lay-data="{field:'district', width:80, sort: true}">市域</th>
            <th lay-data="{field:'note', width:200, sort: true}"></th>
        </tr>
    </thead>
    {% for station in station_list %}
    <tr>
    <td>{{ forloop.counter }}</td>
    <td>{{ station.name_cn }}</td>
    <td>{{ station.code }}</td>
    <td>{{ station.measure_means }}</td>
    <td>{{ station.district }}</td>
<!--    <td><a href="{% url 'manager:stationDetail' station.id %}" ><button>edit</button></a>-->
    <td><button onclick="edit({{ station.id }})">edit</button>
    <button onclick="deleteUnit({{ station.id }})" >delete</button></td>
    </tr>
    {% endfor %}
</table>
{% include './paginator.html' %}
<a href="javascript:history.go(-1);">返回上一页</a>
</div>

{% endblock %}
</div>
